@model WebApplication6.model.EmployprojectviewModel
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    }

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>AboutEmp</title>
</head>
<body>
    <h1>Employ</h1>
    <table border="1">
        <tr>
            <td>Id</td>
            <td>Name</td>
            <td>Email</td>
            <td>Dept</td>
            <td>ProjectName</td>
        </tr>
        <tr>
            <td>@Model.employ.Id</td>
            <td>@Model.employ.Name</td>
            <td>@Model.employ.Email</td>
            <td>@Model.employ.dept</td>
            <td>@Model.projectName</td>
        </tr>
        </table>

</body>
</html>
